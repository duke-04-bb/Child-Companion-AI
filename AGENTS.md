# 项目开发约束

所有项目修改必须严格遵循 [代码规范.md](代码规范.md)。在实现或审查网页代码时，尤其确认：

- HTML 语义、单一 `h1`、图片替代文本和键盘可访问性；
- CSS 的语义化短横线类名、外部样式与响应式表现；
- JavaScript 不使用 `var` 或内联事件，且不保留 `console.log()`；
- 不提交密钥、真实用户资料、无用资源或调试代码；
- Git 提交信息采用 `feat:`、`fix:`、`style:`、`refactor:` 或 `docs:` 前缀。
