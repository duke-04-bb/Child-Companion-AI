# 小师展示网页代码规范

## 基本原则

- 代码应清晰、易读、易维护，优先使用语义化、可复用、简单的实现方式。
- 页面内容、样式与交互逻辑分离；不得保留无效代码、调试代码或无用资源。
- 未经说明，不使用硬编码数据。

## 推荐目录结构

```text
project/
├── index.html
├── css/style.css
├── js/main.js
├── assets/images/
├── assets/icons/
└── README.md
```

## HTML

- 使用 `<!doctype html>`、UTF-8 与 2 空格缩进，属性一律使用双引号。
- 使用 `header`、`nav`、`main`、`section`、`footer` 等语义化标签。
- 每页仅一个 `h1`，标题层级按 `h1`、`h2`、`h3` 顺序使用。
- 图片必须有有意义的 `alt`；按钮使用 `button`，链接使用 `a`，不可用普通 `div` 代替交互元素。

## CSS

- 使用外部样式文件，避免大量内联样式。
- 类名使用小写短横线，如 `.feature-card`；不得使用 `.box1`、`.red-text` 等无语义名称。
- 优先使用 CSS 变量管理颜色、间距、圆角；选择器嵌套不超过 3 层。
- 统一管理响应式断点；动画应柔和且不影响阅读或操作。

## JavaScript

- 使用 `const`、`let`，禁止 `var`；不得将大量逻辑写在 HTML 的 `onclick` 中。
- 变量与函数使用小驼峰；函数以动词命名并只负责一个明确功能；类用大驼峰；常量用大写蛇形。
- 操作 DOM 前确认元素存在；复杂逻辑添加简短注释；正式代码不得有 `console.log()` 调试语句。

## 命名

| 类型 | 规范 | 示例 |
| --- | --- | --- |
| HTML 类名 | 小写短横线 | `.hero-section` |
| JavaScript 变量 | 小驼峰 | `storyText` |
| JavaScript 函数 | 小驼峰加动词 | `renderStory()` |
| JavaScript 类 | 大驼峰 | `EmotionAnalyzer` |
| 常量 | 大写蛇形 | `DEFAULT_EMOTION` |
| 图片文件 | 小写短横线 | `robot-companion.png` |

## 无障碍与安全

- 保持足够的文字背景对比度；主要按钮与导航必须可通过键盘访问且有清晰焦点状态。
- 不在前端写入密钥、密码或真实用户信息。
- 儿童相关内容须经安全过滤，不展示危险、暴力或不适合儿童的内容。
- 产品文案不得宣称 AI 能百分之百准确识别情绪。

## Git 提交与提交前检查

- 提交信息格式为 `类型: 简短说明`；允许类型：`feat`、`fix`、`style`、`refactor`、`docs`。
- 提交前验证：页面在桌面与移动端正常、导航和按钮可用、控制台无报错、资源路径正确、中文无乱码，且不包含密钥、临时文件或调试代码。
